#ifndef _LIST_
#define _LIST_


using namespace std;

#include <vector>

/* compute the average of the list values */
float vector_compute_avg(vector<float>*);

/* compute the standard deviation of the list values */
float vector_compute_stddev(vector<float>*, float);

#endif
